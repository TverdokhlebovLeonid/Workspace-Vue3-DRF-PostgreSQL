# workspace-frontend

Vue 3 SPA для проекта Workspace.

```powershell
npm install
npm run dev
```

API проксируется на `http://127.0.0.1:8000` (см. `vite.config.ts`).

Подробнее — в корневом [README.md](../README.md).
